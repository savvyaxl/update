<tr>
    <td> <b>{{ inventory_hostname | default(' ') }}</b>         </td>
    <ttdr>             
    {% if win_reboot.changed %}
        Updated
    {% else %}
        OK
    {% endif %}
    </td>
    <td>
    {% if win_reboot.reboot_required | default(false) %}
        Reboot Required
    {% else %}
        OK
    {% endif %}
    </td>
    <td> 
    {% if win_reboot.rebooted | default(false) %}
        Rebooted
    {% else %}
        {% if win_reboot.reboot_required | default(false) %}
            Reboot Required
        {% else %}
            OK
        {% endif %}
    {% endif %}
    </td>
</tr>
