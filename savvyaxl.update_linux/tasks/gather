- name: get some facts
  ansible.builtin.setup:
    gather_subset:
      - '!all'
      - dns
      - "machine"
      - hardware
      - "selinux"
      - system
      - "processor_count"
      - "processor_vcpus"

- name: get some facts, debug
  ansible.builtin.debug:
    var: ansible_facts

